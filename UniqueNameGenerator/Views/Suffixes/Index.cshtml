@model IEnumerable<UniqueNameGenerator.Models.Suffix>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Suffix1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Suffix1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SuffixID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SuffixID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SuffixID })
        </td>
    </tr>
}

</table>
