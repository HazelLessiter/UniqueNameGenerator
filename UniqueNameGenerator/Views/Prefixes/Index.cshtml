@model IEnumerable<UniqueNameGenerator.Models.Prefix>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Prefix1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Prefix1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrefixID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrefixID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrefixID })
        </td>
    </tr>
}

</table>
